# WhatsappSmsSpoof
send automatic sms spoofs to anyone's Whatsapp. Educational perpous only!!!

**You can send unlimited sms spoofs using this code. Don't use it for illegal perpouses**

# Installation

```bash
apt update
```
```bash
apt install python3
```
```bash
sudo apt install python3
```
```bash
pip install git
```
```bash
python3 -m pip install requests
```
Please try using this command instead If you encounter an "externally managed environment" error when running the earlier command:
```bash
python3 pip install requests --break-system-packages
```
Note: It is generally recommended to use a Virtual Environment (venv) rather than forcing a global installation.

```bash
git clone https://github.com/blackarchparrot/WhatsappSmsSpoof.git
```
```bash
cd WhatsappSmsSpoof
```
```bash
python3 spoof.py
```
select = 4- Tokopedia Mode=1 Phone No= +8801570....... Delay=1

Mobile No should be in international format

+8801..............

